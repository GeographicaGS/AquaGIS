'use strict';

App.View.Panels.Maintenance.Master = App.View.Panels.Base.extend({

  initialize: function(options) {
    // Redirect to URL APP
    window.location.href = 'https://aquasig-app.geographica.gs'
  }
});
